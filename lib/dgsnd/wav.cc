#include "wav.hh"

namespace DG {
namespace Sound {

void WAV::open(const std::string& fname) {
  std::move(fname); // do fun things until we define this function
}

void WAV::write(const std::string& fname) {
  std::move(fname); // do fun things until we define this functio
}

}
}
