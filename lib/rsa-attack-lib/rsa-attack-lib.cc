#include <iostream>
#include "rsa-attack-lib.h"

using namespace std;
vector<int> d_int;

vector<mpz_class> findFactors(mpz_class n){
  vector<mpz_class> factors;
  //trial division method...
  // mpz_class i =
  // while(mpz_t i < (mpz_t i = 2_mpz; i < sqrt(n); ++i) { //ignore 0 and 1, since they're not valid factors
  //   if((n % i) == 0) {
  //     factors.push_back(i);
  //   }
  // }
  return factors;
}
